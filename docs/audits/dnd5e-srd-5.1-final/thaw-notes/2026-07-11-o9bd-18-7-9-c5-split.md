# Thaw Note — C5 `splitOnDamage` projection

## Reason for thaw

Implement bead `eshyra-o9bd.18.7.9.11`, slice C5 of the reviewed creature-entry
classification. The two Split reactions were fail-closed findings because a
bare trigger marker did not represent the deterministic reproduction result.

## Exact refs

- `creature:black-pudding#reactions:Split`
- `creature:ochre-jelly#reactions:Split`

Both now receive the closed `splitOnDamage` payload:

```json
{
  "kind": "splitOnDamage",
  "damageTypes": ["lightning", "slashing"],
  "minimumSize": "medium",
  "minimumHitPoints": 10,
  "resultingCreatureCount": 2,
  "hitPointsFraction": "half-rounded-down",
  "sizeCategoriesDown": 1
}
```

## Membership/readiness delta

Before: 72 reviewed creature refs, 18 registry entries consisting of 2
permanent accepted-prose refs and 16 pending findings; C5 contributed 2 pending
findings. After: 72 reviewed refs, 16 registry entries consisting of the same
2 accepted-prose refs and 14 pending findings. C5 contributes zero pending
findings. The remaining findings are C4 2, C6 4, C7 2, C8 2, and C9 4.

## Source and generated-pack evidence

The importer recognizes only the complete current SRD source strings for the
pudding and jelly nouns, including all damage, size, HP threshold, two-result,
half-rounded-down, and one-size-smaller clauses. Any changed or truncated
clause fails closed and does not emit `splitOnDamage`. The specific effect also
suppresses the disconnected generic `triggeredEffect` marker.

Regeneration changes exactly the existing `creature:black-pudding` and
`creature:ochre-jelly` records. No records are added or removed, and no
unrelated creature, spell, record-count, or mechanics projection changes.

## Verification

- Focused Vitest set: **174 passed**.
- `npm run import:dnd5e-srd -- --out packages/core/data/rules-packs/rules__dnd5e-srd-5.1`: completed; expected two-record diff.
- `npm run verify:dnd5e-srd-pack`: passed.
- `npm run verify:dnd5e-srd-freeze`: passed; all 13 hashes match.
- `npm run audit-bundle:dnd5e-srd`: passed; 0 playability findings and 0
  structure/coverage findings. Its nested `check`, `typecheck`, `test`, and
  pack verification commands all passed.
- `npm run check`: passed.
- `npm run typecheck`: passed.
- `npm run verify:worktree`: passed; 3,732 tests passed and 19 documented
  skips.

The normal freeze workflow remains active; the committed freeze manifest is
updated only for the regenerated pack hash.

Source PDF and source manifest are unchanged.
