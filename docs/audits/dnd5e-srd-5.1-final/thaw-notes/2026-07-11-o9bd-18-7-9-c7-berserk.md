# Thaw Note — C7 `berserk` state-machine projection

## Reason for thaw

Implement bead `eshyra-o9bd.18.7.9.13`, slice C7 of the reviewed
creature-entry classification. The two Berserk traits previously retained only
a disconnected trigger marker, which did not represent their deterministic
low-HP d6 entry, persistent behavior, exits, or Flesh Golem calming clause.

## Exact refs

- `creature:clay-golem#traits:Berserk`
- `creature:flesh-golem#traits:Berserk`

Both now receive the closed `berserk` state machine with a calm initial state,
a start-of-turn low-HP `d6` entry whose only entering result is `6`, an
each-turn berserk self-transition that attacks the nearest visible creature
(or, when none is near enough to move to and attack, a smaller object), and
destroyed/full-heal exits. Flesh Golem additionally receives the
creator-only, 60-foot, hearing-gated action-cost DC 15 Charisma (Persuasion)
calming exit and a source-qualified, model-adjudicated low-HP damage re-entry
eligibility clause.

## Membership/readiness delta

Before: 72 reviewed creature refs, 12 registry entries consisting of 2
permanent accepted-prose refs and 10 pending findings; C7 contributed 2
pending findings. After: 72 reviewed refs, 10 registry entries consisting of
the same 2 accepted-prose refs and 8 pending findings. C7 contributes zero
pending findings. The remaining findings are C4 2, C8 2, and C9 4.

## Source and generated-pack evidence

The importer recognizes only the two complete current SRD source strings.
Every transition is source-bound, including the d6/6 entry, target-selection
fallback, destroyed/full-heal exits, and each Flesh Golem calming constraint.
The source says its post-calming damage clause only *might* cause berserk
again, without restating a die result; the re-entry eligibility records that
clause as `model-adjudicated`, rather than asserting an executable
`calm`→`berserk` transition or inventing another d6 rule. Changed or truncated
clauses fail closed and do not emit `berserk`; the specific effect also
suppresses the disconnected generic `triggeredEffect` marker.

Regeneration is expected to change exactly the two existing creature records
above. No records should be added or removed, and no unrelated creature,
spell, record-count, or mechanics projection should change.

Source PDF and source manifest are unchanged.
