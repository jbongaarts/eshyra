# Thaw Note — C8 `triggeredBonusAction` Rampage projection

## Reason for thaw

Implement bead `eshyra-o9bd.18.7.9.14`, slice C8 of the reviewed
creature-entry classification. The two Rampage traits previously retained only
a disconnected trigger marker, which did not represent the trigger-gated bonus
action or its linked movement-and-bite result.

## Exact refs

- `creature:giant-hyena#traits:Rampage`
- `creature:gnoll#traits:Rampage`

Both now receive the closed `triggeredBonusAction` contract. Its trigger
requires reducing a creature to 0 hit points with a melee attack on the
actor's turn; its one action requires movement up to half speed and a bite
attack. The schema rejects unlinked extra fields and keeps the trigger and
result in a single object, never related by array adjacency.

## Membership/readiness delta

Before: 72 reviewed creature refs, 10 registry entries consisting of 2
permanent accepted-prose refs and 8 pending findings; C8 contributed 2 pending
findings. After: 72 reviewed refs, 8 registry entries consisting of the same 2
accepted-prose refs and 6 pending findings. C8 contributes zero pending
findings. The remaining findings are C4 2 and C9 4.

## Source and generated-pack evidence

The importer recognizes only the complete current SRD Rampage sentence for the
hyena and gnoll. A changed attack type, bite attack, speed fraction, or actor
reference fails closed and does not emit `triggeredBonusAction`; the specific
effect also suppresses the disconnected generic `triggeredEffect` marker.

Regeneration is expected to change exactly the two existing creature records
above. No records should be added or removed, and no unrelated creature,
spell, record-count, or mechanics projection should change.

Source PDF and source manifest are unchanged.

## Expected file changes

- [x] `packages/core/scripts/importers/dnd5e-srd-5.1/` — exact, fail-closed
  Rampage grammar.
- [x] `packages/core/data/rules-packs/rules__dnd5e-srd-5.1/records.json` —
  exactly Giant Hyena and Gnoll Rampage projections.
- [x] `docs/audits/dnd5e-srd-5.1-final/` — membership reconciliation, this
  thaw note, and refreshed freeze hash.

## Freeze manifest updated

`freeze-manifest.json` records the regenerated `records.json` SHA-256:
`4f3c045cb532adcd2879410cc2d25c541dc2633febea2da0c71d606d0f2ce35c`.

## Audit bundle path

Verified bundle: `/tmp/eshyra-o9bd-18-7-9-14-audit-bundle`
(`.zip` sibling generated successfully).
