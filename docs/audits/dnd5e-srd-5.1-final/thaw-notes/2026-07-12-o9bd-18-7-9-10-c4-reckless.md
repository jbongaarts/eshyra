# C4 Reckless creature behavior thaw

Date: 2026-07-12. Bead: `eshyra-o9bd.18.7.9.10`.

Reason for thaw: land the reviewed C4 semantic projection for the two SRD
creature traits that were intentionally held as pending findings.

Exact refs:

- `creature:berserker#traits:Reckless`
- `creature:minotaur#traits:Reckless`

Both now emit this closed payload:

```ts
{
  kind: 'recklessAttack',
  activation: { timing: 'start-of-turn', optional: true },
  benefit: {
    rolls: 'all-melee-weapon-attack-rolls',
    mode: 'advantage',
    duration: 'current-turn',
  },
  tradeoff: {
    rolls: 'all-attack-rolls-against-self',
    mode: 'advantage',
    duration: 'until-start-of-next-turn',
  },
}
```

The benefit and tradeoff are one linked optional bargain. Keeping them in one
object preserves the source relationship and prevents runtime consumers from
mistaking two adjacent independent modifiers for an elected trade.

The source gate is fail-closed: it requires the exact complete sentence, the
allowlisted Berserker/Minotaur actor noun, the optional start-of-turn choice,
both roll scopes, both advantage clauses, and both duration boundaries. Source
drift emits no `recklessAttack` effect.

Membership/readiness delta: the reviewed disposition registry went from 4 to
2 entries; pending creature findings went from 2 to 0; the 2 permanent
accepted-prose refs were unchanged.

The generated-pack delta is exactly two changed existing records (Berserker
and Minotaur), with zero records added or removed and unchanged per-kind
counts. The source PDF and source manifest are unchanged. The regenerated
audit-bundle commands use:

```text
npm run import:dnd5e-srd
npm run verify:dnd5e-srd-pack
npm run verify:dnd5e-srd-freeze -- --base origin/main
npm run audit-bundle:dnd5e-srd -- /tmp/eshyra-o9bd-18-7-9-10-c4-audit-bundle /tmp/eshyra-o9bd-18-7-9-10-c4-audit-bundle.zip
```

Audit-bundle output paths: `/tmp/eshyra-o9bd-18-7-9-10-c4-audit-bundle`,
`/tmp/eshyra-o9bd-18-7-9-10-c4-audit-bundle.zip`, and the copied archive
`/tmp/eshyra-o9bd-18-7-9-10-c4-audit-bundle-copy.zip`.

Resulting frozen `records.json` SHA-256:
`516ed75c42905e9d230ba69a81a72f64638c1b79f9c1bbe411c8974768a3e16b`.
